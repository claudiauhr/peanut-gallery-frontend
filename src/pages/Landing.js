// importing Routes
import Header from "../components/Header";

// importing pagescomponents



const Landing = (props) => {
    return (
        <main>
            <Header />
        </main>
    );
};

export default Landing;